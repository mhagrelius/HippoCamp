name: PR Validation

on:
  pull_request:
    types: [opened, synchronize, reopened]
  merge_group:
    types: [checks_requested]

permissions:
  contents: read
  pull-requests: read

jobs:
  commitlint:
    name: Validate Conventional Commits
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      with:
        fetch-depth: 0
    
    - name: Lint Commit Messages
      uses: wagoid/commitlint-github-action@v6
      with:
        configFile: commitlint.config.mjs
        failOnWarnings: false
        failOnErrors: true

  git-flow-validation:
    name: Validate Git-Flow Rules
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
    - name: Check Branch Strategy Compliance
      run: |
        echo "🔍 Validating Git-Flow compliance..."
        echo "Source branch: ${{ github.head_ref }}"
        echo "Target branch: ${{ github.base_ref }}"
        
    - name: Check Branch Strategy Compliance
      env:
        HEAD_REF: ${{ github.head_ref }}
        BASE_REF: ${{ github.base_ref }}
      run: |
        # Validate Git-flow branch targeting patterns
        HEAD_REF="${HEAD_REF:?}"
        BASE_REF="${BASE_REF:?}"
        echo "🔍 Source: ${HEAD_REF} → Target: ${BASE_REF}"

        if [[ "${BASE_REF}" == "main" ]]; then
          # PRs to main: only from develop (releases) or hotfix/* (emergencies)
          if [[ "${HEAD_REF}" == "develop" ]]; then
            echo "✅ Release PR from develop to main - allowed"
          elif [[ "${HEAD_REF}" =~ ^hotfix/.+ ]]; then
            echo "✅ Emergency hotfix PR to main - allowed"
          elif [[ "${HEAD_REF}" =~ ^release/.+ ]]; then
            echo "✅ Release branch PR to main - allowed"
          else
            echo "❌ Invalid PR to main!"
            echo "💡 Only allowed: develop (releases), hotfix/* (emergencies), release/* (release prep)"
            exit 1
          fi
        elif [[ "${BASE_REF}" == "develop" ]]; then
          # PRs to develop: only from feature/* 
          if [[ "${HEAD_REF}" =~ ^feature/.+ ]]; then
            echo "✅ Feature branch targeting develop - allowed"
          elif [[ "${HEAD_REF}" =~ ^release/.+ ]]; then
            echo "✅ Release branch back-merge to develop - allowed"
          elif [[ "${HEAD_REF}" =~ ^hotfix/.+ ]]; then
            echo "✅ Hotfix back-merge to develop - allowed"
          else
            echo "❌ Invalid branch name for develop target!"
            echo "💡 Use 'feature/#{issue}-description' for features"
            echo "📋 Invalid branch: ${HEAD_REF}"
            exit 1
          fi
        fi
        
        echo "✅ Git-flow branch strategy is valid"

  forbid-merge-commits:
    name: Forbid Merge Commits in PRs
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request' && github.base_ref != 'main'
    steps:
    - name: Forbid merge commits
      uses: motlin/forbid-merge-commits-action@main

  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4
      
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'
        
    - name: Restore dependencies
      run: dotnet restore
      
    - name: Build solution
      run: dotnet build --no-restore --configuration Release
      
    - name: Run tests
      run: dotnet test --no-build --configuration Release --verbosity normal
